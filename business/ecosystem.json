{
    "apps": [{
        "name": "flightSeeker",
        "script": "./bin/www",
        "cwd": "./",
        "exec_mode": "fork_mode"
    }],
    "deploy": {
        "production": {
            "post-deploy": "npm install",
            "host": ["127.0.0.1"]
        }
    }
}